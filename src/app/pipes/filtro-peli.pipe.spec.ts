import { FiltroPeliPipe } from './filtro-peli.pipe';

describe('FiltroPeliPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPeliPipe();
    expect(pipe).toBeTruthy();
  });
});
