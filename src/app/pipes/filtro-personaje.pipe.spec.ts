import { FiltroPersonajePipe } from './filtro-personaje.pipe';

describe('FiltroPersonajePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPersonajePipe();
    expect(pipe).toBeTruthy();
  });
});
